# 🔐 PasswordKeeper

Простое десктопное приложение для генерации, шифрования и хранения паролей. Разработано на Python с использованием PyQt6 (или PySide6 для совместимости).

## Описание
Приложение объединяет три ключевые функции:
### 🔑 Генератор паролей
- Генерация случайных паролей с настройкой длины и состава.  
- Используется модуль `secrets` — криптографически безопасный источник случайных чисел.
### 🧩 Шифратор/дешифратор
- Алгоритм Цезаря и Виженера позже AES-256 (через `cryptography.fernet`).  
- Все операции требуют мастер-пароля.  
- Можно шифровать произвольный текст, не только пароли.  

### 💾 Хранилище паролей
- Данные сохраняются в `passwords.txt` (зашифрованный JSON).  
- Поддержка операций: создание, просмотр, поиск, редактирование, удаление.  
- Полностью локальная работа без доступа к интернету. 

PasswordKeeper/
│
\
├── Grafics.py # GUI и логика вкладок
\
├── PasswordGen.py # Генерация паролей
\
├── Shifrator.py # Шифрование и дешифрование
\
├── PasswordManager.py # Работа с хранилищем
\
├── resources.py # Подключение иконок
\
├── style.css # Стили интерфейса
\
├── icons/ # Иконки интерфейса
\
└── passwords.txt # Локальная база

## Требования
- Python 3.8+
- PyQt6 (или PySide6 но нужно изменить импорты)
- secrets
- cryptography
- passlib


## Установка
1. Клонируйте репозиторий: `git clone https://github.com/Magmucot/PasswordKeeper.git`
2. Установите зависимости `pip install secrets passlib cryptography PyQt6`
3. Запустите: `python Grafics.py`

## Использование

#### 1.При первом запуске задайте мастер-пароль.

#### 2.Вкладка Generator — создание паролей.

#### 3.Вкладка Cipher — шифрование и дешифрование текста.

#### 4.Вкладка Storage — управление записями (CRUD).


## 📜 Лицензия

### MIT License © 2025 Magmucot